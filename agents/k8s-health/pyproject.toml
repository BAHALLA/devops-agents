[project]
name = "k8s-health-agent"
version = "0.1.7"
description = "Agent for monitoring and managing Kubernetes clusters"
requires-python = ">=3.14"
dependencies = [
    "orrery-core",
    "kubernetes>=31.0.0",
]

[tool.uv.sources]
orrery-core = { workspace = true }
