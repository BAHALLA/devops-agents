# Docker agent configuration
# DOCKER_HOST=unix:///var/run/docker.sock   # defaults to local Docker socket

# LLM provider (pick one)
# GOOGLE_API_KEY=your-google-api-key
# MODEL_PROVIDER=gemini
# MODEL_NAME=gemini-2.0-flash
