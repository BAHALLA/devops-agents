[project]
name = "docker-agent"
version = "0.1.7"
description = "Agent for managing Docker containers and images"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "orrery-core",
]

[tool.uv.sources]
orrery-core = { workspace = true }
