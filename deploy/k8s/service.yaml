apiVersion: v1
kind: Service
metadata:
  name: orrery-assistant
  namespace: orrery
  labels:
    app.kubernetes.io/name: orrery-assistant
    app.kubernetes.io/component: agent
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: orrery-assistant
  ports:
    - name: http
      port: 8000
      targetPort: http
      protocol: TCP
    - name: metrics
      port: 9100
      targetPort: metrics
      protocol: TCP
